import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Text("Hello AutoProCam16!")
                .font(.largeTitle)
                .padding()
            Text("Minimal iOS Project for GitHub Actions")
                .foregroundColor(.gray)
        }
    }
}

#Preview {
    ContentView()
}